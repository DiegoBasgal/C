#include <stdio.h>

void troca (int* p1, int* p2);


int main () {
    int x = 10;
    int y = 20;
    /* você deve identificar como fazer a
    passagem dos parâmetros */
    swap(...);
    /* deve imprimir x = 20, y = 10 */
    printf("x = %d, y = %d\n", x, y);
    return 0;
}
